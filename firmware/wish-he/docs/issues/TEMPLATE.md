# NNN — <사용자가 보는 증상 한 줄>

| | |
|---|---|
| 이슈 | <https://github.com/chcbaram/wish-he/issues/NNN> |
| 신고 | YYYY-MM-DD, `<github 아이디>` |
| 신고된 환경 | 61키 HE 보드, 펌웨어 `vYYMMDDRn` |
| 분석한 소스 | `<커밋>` (`VYYMMDDRn`) |
| 상태 | 분석 끝, 미수정 |
| 자리 | `<함수>`, `src/.../file.c` |

## 증상

보고자가 본 것을 그 사람의 말과 숫자 그대로 적는다.

## 무슨 일이 일어나는가

차례대로, 그것을 정하는 코드와 함께. 중요한 몇 줄만 인용한다 — 함수를 통째로
붙이지 않는다.

```c
/* src/.../file.c:NNNN */
```

문장마다 표시한다 — **장치로 잰 것**인지 **코드에서 읽은 것**인지.

## 왜 지금까지 안 드러났나

가리고 있던 설정이나 경로. 그 자리를 덮는 시험이 있는데도 통과한다면 왜 통과하는지
적는다.

## 고칠 자리

버티는 가장 작은 수정과, 거기 쓰인 상수 하나하나의 근거.

```c
```

### 생각했다가 접은 길

| 대안 | 왜 안 되나 |
|---|---|
| | |

## 시험

`tools/he_test.py` 에 들어갈 재현 절차와, 안 고친 펌웨어에서 그게 어떻게 실패하는지.

## 굽기 전후로 재는 것

`keys time`, `qmk reset` 뒤 `qmk info`, 그리고 이 이슈가 건드리는 것.

## 다른 세션에서 집을 수 있는 것

| 할 일 | 하드웨어 | 나오는 것 |
|---|---|---|
| | 필요/불필요 | |

## 아직 안 잰 것

닫기 전에 실물로 확인해야 하는 것.
